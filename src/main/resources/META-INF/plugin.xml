<idea-plugin>
    <id>com.ccoder.claudecode</id>
    <!-- 名字的**真正来源**是 gradle.properties 的 pluginName（patchPluginXml 会拿它
         覆盖这一行）。两处都写同样的值，免得看 plugin.xml 的人以为改这里就行。
         括号是市场指南 ✅ 栏里点名允许的符号；带上 "Claude Code" 是为了能搜到
         （2026-09-15，理由见 docs 里那次搜索实测）。 -->
    <name>CCoder (Claude Code Assistant)</name>
    <!-- 市场审核指南要"vendor 网站有效可访问、邮箱有效"。邮箱填了（git 里的提交邮箱）；
         url 仍空着。注：仓库 2026-09-17 已确认公开（github.com/princeyuaner/CCoder，
         市场 Technical Information 里的 source code 链接就是它），要挂主页可以从这里填；
         不填也不缺什么。名字 ccoder 是插件页上显示的那个。 -->
    <vendor email="594954848@qq.com">ccoder</vendor>

    <!-- 描述在**两处**：这里是源码侧的权威副本（随包发上去），
         docs/marketplace-description.md 是同一份的 Markdown —— 市场后台
         （General Information → Description）从 2025-09 起可以直接改描述，
         **不用重传包**，而那个编辑器只吃 Markdown。两处改一处，另一处跟着改。
         口径：英文在前（指南要求首 40 字符是英文，预览卡就取这一段）、
         中英各一份（§八）。**不写 <h1>~<h6>** —— 后台存的是 HTML，标题标签
         在论坛上有出问题的报告，粗体小标题一样的用。 -->
    <description><![CDATA[
    <p>Claude Code in a PyCharm tool window: chat against the project you are editing,
    see every file edit as a diff before it lands, and approve the commands that touch
    your machine.</p>

    <p>CCoder runs the Claude Code CLI (<code>claude</code>) you already have installed
    &mdash; it is not another AI backend and ships no key of its own. It works on the
    project you have open: its files, its <code>CLAUDE.md</code>, its settings.</p>

    <p><b>Working in the project</b></p>
    <ul>
      <li><code>@</code> references files and the code you selected in the editor (both
          show as one-line tokens and expand when sent); <code>#</code> references
          project symbols such as classes and functions. Completion is fuzzy &mdash;
          <code>cmprk</code> finds <code>ComposerMode.kt</code>.</li>
      <li><code>/</code> opens slash commands and the preset prompts you saved.</li>
      <li>Ctrl+V pastes a screenshot straight into the input box &mdash; click a
          thumbnail to open it full size.</li>
      <li>Type while Claude is working and your message is queued, sent when the turn ends.</li>
    </ul>

    <p><b>Changes you can see &mdash; and stop</b></p>
    <ul>
      <li>Every file edit is shown as a diff before it is applied.</li>
      <li>Commands that touch your machine (shell, writes outside the project) wait for
          your Allow or Deny.</li>
      <li>Six permission modes, from read-only Plan and Auto-accept-edits to a
          classifier-driven Auto that judges each prompt and still asks when unsure.</li>
      <li>Plan approvals are laid out as Markdown (tables included), in dialogs you can
          resize.</li>
      <li>Running subagents and background commands can be stopped one by one (Agents
          popup &mdash; the small &#9632; at the end of a row). Only that task stops;
          the conversation goes on.</li>
    </ul>

    <p><b>Sessions</b></p>
    <ul>
      <li>Up to five sessions at once in one window; each has its own chip, with a status
          dot for running / waiting on you / idle.</li>
      <li>Sessions are named after your first message; resume, rename or delete them from
          the list. Closing a tab that is still working asks first.</li>
    </ul>

    <p><b>Bring your own model</b> &mdash; choose the model and the thinking effort;
    model profiles point at your own gateway or endpoint.</p>

    <p><b>Project configuration</b></p>
    <ul>
      <li>MCP panel: read and edit the project's <code>.mcp.json</code>, with the live
          connection status of every server.</li>
      <li>Hooks panel: read and edit hooks in the project's <code>.claude/settings.json</code>
          &mdash; nothing else in that file is touched.</li>
    </ul>

    <p><b>Requirements</b></p>
    <ul>
      <li>PyCharm 2025.3 or newer.</li>
      <li>Claude Code CLI (<code>claude</code>) &mdash; <b>not bundled</b>; CCoder drives
          the copy you installed.</li>
      <li>Node.js.</li>
      <li>An Anthropic account, subscription or API key &mdash; whatever the CLI itself uses.</li>
    </ul>

    <p><b>Notes</b></p>
    <ul>
      <li>The plugin talks only to the CLI it launches; your code and prompts go where
          that CLI is configured to send them.</li>
      <li>The interface language can be switched in the plugin's settings (gear icon in
          the tool window &rarr; General): follow the IDE language, Chinese, or English.
          Claude's replies follow your prompt's language.</li>
      <li>Not affiliated with Anthropic. "Claude" and "Claude Code" are trademarks of
          Anthropic PBC.</li>
      <li>MIT licensed. Source and issue tracker:
          <a href="https://github.com/princeyuaner/CCoder">github.com/princeyuaner/CCoder</a></li>
    </ul>

    <p><b>中文</b></p>

    <p>在 PyCharm 的工具窗口里跑 Claude Code：就着你正在编辑的项目提问，每次改文件都先给你看
    diff，会动你机器的命令由你放行或拒绝。</p>

    <p>CCoder 驱动的是你自己已经装好的 Claude Code CLI（<code>claude</code>）&mdash;&mdash;
    它不是另一个 AI 后端，也不自带任何密钥。它就在你打开的那个项目里干活：项目里的文件、
    它的 <code>CLAUDE.md</code>、它的设置。</p>

    <p><b>在这个项目里干活</b></p>
    <ul>
      <li><code>@</code> 引用文件，以及在编辑器里选中的代码（都显示成一行记号，发送时才展开）；
          <code>#</code> 引用项目里的类、函数等符号。补全是模糊的 &mdash;&mdash; 敲
          <code>cmprk</code> 也能找到 <code>ComposerMode.kt</code>。</li>
      <li><code>/</code> 打开斜杠命令，以及你在设置里存下的预置 prompt。</li>
      <li>Ctrl+V 把截图贴进输入框，点缩略图能看大图。</li>
      <li>Claude 干活时你照样能打字：消息排进队列，当前这轮一结束就发出去。</li>
    </ul>

    <p><b>看得到、也拦得住的改动</b></p>
    <ul>
      <li>每次改文件都先给你看 diff。</li>
      <li>会动你机器的命令（shell、项目外的写入）由你放行或拒绝。</li>
      <li>六种权限模式，从只读的「仅规划」、「自动接受编辑」，到由模型逐条判定的
          「自动判定」&mdash;&mdash; 拿不准的仍会询问。</li>
      <li>计划审批按 Markdown 排版（表格也是表格），框可以拉伸。</li>
      <li>在跑的子代理与后台命令可以逐个终止（「子代理」浮层 &rarr;「运行中」&rarr;
          行右端那颗 ■），停的只是那一个，聊天继续。</li>
    </ul>

    <p><b>会话</b></p>
    <ul>
      <li>一个窗口里最多同时开 5 条会话；每条一颗胶囊，一颗状态点在跑 / 等你 / 空闲。</li>
      <li>会话名取你说的第一句；列表里可恢复、改名、删除。关掉还在干活的标签会先问一句。</li>
    </ul>

    <p><b>自带模型</b> &mdash;&mdash; 选模型与思考档位；模型配置（profile）还能指向你自己的
    网关或端点。</p>

    <p><b>项目配置</b></p>
    <ul>
      <li>MCP 面板：读写项目级 <code>.mcp.json</code>，每个 server 的连接状态实时可见。</li>
      <li>hooks 面板：读写项目级 <code>.claude/settings.json</code> 里的 hooks &mdash;&mdash;
          同文件别的内容一个字节不动。</li>
    </ul>

    <p><b>需要什么</b></p>
    <ul>
      <li>PyCharm 2025.3 或更新版本。</li>
      <li>Claude Code CLI（<code>claude</code>）&mdash;&mdash; <b>不随插件分发</b>，
          驱动的是你已经装好的那份。</li>
      <li>Node.js。</li>
      <li>CLI 本身要的东西：Anthropic 账号 / 订阅 / API key。</li>
    </ul>

    <p><b>说明</b></p>
    <ul>
      <li>界面语言可切（工具窗口右上角齿轮 &rarr; 通用）：跟随 IDE、中文、English，默认跟随
          IDE。Claude 的回复跟着你 prompt 的语言走。</li>
      <li>插件只和它启动的那个 CLI 说话；你的代码与 prompt 去向由那个 CLI 的配置决定。</li>
      <li>与 Anthropic 无关；"Claude" 与 "Claude Code" 是 Anthropic PBC 的商标。</li>
      <li>MIT 许可。源码与问题反馈：
          <a href="https://github.com/princeyuaner/CCoder">github.com/princeyuaner/CCoder</a></li>
    </ul>
    ]]></description>

    <!-- 口径：**只写相对上一版（0.2.25）的增量**，中英各一份（§八）。
         0.2.26 **没上架** —— 市场的 Plugin Verifier 拦在 internal API 上（我用了
         `JBCefApp.getNativeBundleVersionString()`，它带 `@ApiStatus.Internal`；
         审核指南里那条是拦路不是提示，见发布设计稿 §七）。已改成 `org.cef` 的公开 API，
         0.2.27 重发。所以这一版**不是相对 0.2.26 的增量，而是 0.2.26 那七条原样带出** ——
         用户从没拿到过它们（这就是当初留的那条应急方案）。
         那七条在 tag `v0.2.26` → 98b3b32 那棵树的 plugin.xml 里。复核法见发布设计稿 §五。 -->
    <change-notes><![CDATA[
    <h3>0.2.27</h3>
    <ul>
      <li><b>The conversation view survives a broken embedded browser</b> &mdash; IntelliJ
          2025.2+ runs JCEF in a separate process, and on the JCEF build that 2026.2 ships
          that channel drops (JBR-9234, an NPE inside RemoteMessageRouterImpl). The view
          used to stop updating mid-session, or take the whole new session down with an
          exception dialog. It now degrades to a page with a Retry; a heartbeat catches a
          channel that died without saying so; and when the IDE is in that mode the page
          offers a one-click way to turn it off (which needs a full IDE restart). A session
          opens with a heads-up first &mdash; you hear about it before the freeze, not
          after.</li>
      <li><b>Fixed: Shift+Enter makes a newline again</b> &mdash; the input's key map had
          only plain Enter bound to a line break, and Shift+Enter was left to a character
          event that the IDE drops. It is now bound to exactly the same action as plain
          Enter.</li>
      <li><b>Fixed: the input area scrolls once the text outgrows it</b> &mdash; past the
          visible rows there was no scrollbar, so the rest of what you typed was
          unreachable. It fills the area while the text fits and hands over to a scrollbar
          when it does not.</li>
      <li><b>Sessions are cards now</b> &mdash; the current one is marked with an accent bar
          and a bold title, sessions already open elsewhere are dimmed, and eight fit on a
          screen where ten used to.</li>
      <li><b>Long tool output no longer chokes the view</b> &mdash; a tool result over 8000
          characters is truncated with its original length noted, and very large bodies are
          pushed in chunks instead of as one script.</li>
      <li><b>Model keys can be copied</b> &mdash; a button beside the key field copies the
          whole value even while it is masked, and confirms with a checkmark. You no longer
          have to reveal the secret in order to copy it.</li>
      <li><b>Requires IntelliJ 2026.1 or newer</b> &mdash; the plugin is now built against
          2026.1, where it used to target 2025.3. Older IDEs will not be offered this
          update.</li>
    </ul>

    <h4>中文</h4>
    <ul>
      <li><b>内嵌浏览器坏掉时，对话区扛得住了</b> —— IntelliJ 2025.2 起把 JCEF 放在独立
          进程里，而 2026.2 捆的那版 JCEF 上那条通道会断（JBR-9234，
          RemoteMessageRouterImpl 里的空指针）。从前表现为"用着用着输出区不动了"，
          或者干脆把整个新会话一起带崩成一个异常对话框。现在会降级成一张带「重试」的
          说明页；心跳能发现"断得一声不吭"那种；跑在那个模式上时，页上还给一颗一键关掉
          它的键（改完要完全重启 IDE）。会话打开时先提醒一句 —— 在卡死之前说，而不是之后。</li>
      <li><b>修好：Shift+Enter 又能换行了</b> —— 输入框的键表里只有裸 Enter 绑了换行，
          Shift+Enter 靠的是一个会被 IDE 丢掉的字符事件。现在它绑的是和裸 Enter
          <b>同一个</b>动作。</li>
      <li><b>修好：文本装不下时输入框出滚动条</b> —— 从前超出可视区的那部分既看不见也够不着。
          现在装得下就撑满，装不下就交给滚动条。</li>
      <li><b>会话列表改成卡片</b> —— 当前会话有强调条和加粗标题，已在别处打开的会话变淡，
          一屏 8 条（原来 10 条）。</li>
      <li><b>超长的工具输出不再把界面压死</b> —— 工具结果超过 8000 字会截断并标注原长，
          特别大的正文分片推送。</li>
      <li><b>模型密钥可以复制了</b> —— 密钥框旁边一颗键，打码状态下也照复制整条，点完变对勾。
          不必为了复制先把它显示在屏幕上。</li>
      <li><b>要求 IntelliJ 2026.1 或更新</b> —— 编译目标从 2025.3 抬到了 2026.1。
          更老的 IDE 不会收到这次更新。</li>
    </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <!-- 转写区用内嵌浏览器渲染。该别名自 2025.3.1 起稳定。 -->
    <depends>com.intellij.modules.jcef</depends>

    <!-- 词表：**只**给下面那四个 action 的 %key 用。
         插件自己的文案一律走 `CcoderText`（自己指定 Locale，见 com.ccoder.text），
         因为平台这套查找只认 IDE 语言，认不了设置里那个「界面语言」——
         而 Find Action 的搜索索引的是**模板文本**，运行时改写管不到它，
         所以这一条不能省（两条腿各干一半，见设计稿 §4.3）。

         **顶层元素**：`resource-bundle` 是允许写在顶层的（实测 IDE 自带的 21 个描述符
         全这么写）；服务 EP 则**必须**在 `<extensions>` 里 —— 两者别混。
         `PluginXmlShapeTest` 把这份名单钉住了。 -->
    <resource-bundle>messages.CcoderBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">

        <!--
          设置**没有**注册在这里，是刻意的（2026-09-15 删掉了那个 projectConfigurable）。
          全部设置都在工具窗口右上角那个齿轮里（`com.ccoder.settings.SettingsDialog`，
          八个页签：模型 / 预置 / 通用 / 权限 / 环境 / MCP / hooks / 群交流）—— 从前这里有一页 IDE 设置，
          与对话框各管一半字段，用户得记"哪项在哪儿"。

          代价知道：IDE Settings 的搜索框里再也搜不到 CCoder。而齿轮**不随忙闲置灰**，
          所以"启动失败时进不去改 claudePath"这条链没断（见 ClaudePanel.settingsButton）。
        -->

        <!-- 图标是自己的（2026-09-15）：小 C 套大 C，与插件图标
             （META-INF/pluginIcon.svg）同一张脸。13px 那份不带方块底、
             内圈与外壳同色，理由见 icons/ccoderToolWindow.svg 的注释。
             深色主题那份走 _dark 后缀，平台自己按主题挑。 -->
        <!-- canCloseContents **刻意保持 false**（2026-09-16）：
             标签条与关闭叉改由面板自己画（`SessionChips` 那排胶囊），平台的
             ✕ 与我们自己的 ✕ 同时出现就重复了。关闭逻辑一个字没变 —— 我们那颗 ✕
             走的还是 `ContentManager.removeContent`，于是 `contentRemoveQuery`
             那道闸（忙 / 有待决权限 / 正在启动时先问一句）照旧生效。
             这个标志也**只能写在这儿**：`ToolWindow` 接口上只有 getter，
             `ToolWindowImpl` 里是 `private final boolean`。 -->
        <toolWindow
            id="CCoder"
            anchor="right"
            icon="/icons/ccoderToolWindow.svg"
            factoryClass="com.ccoder.ClaudeToolWindowFactory"/>

        <!-- 待决权限在状态栏常驻显示。
             2026-09-14 改成模态框之后，§6.3 原本那两条补偿（粘性通知、
             待决超时提醒）连同 "CCoder Permissions" 通知组一起删掉了：
             框会自己弹到眼前，"被忽略"这件事在模态形态下不存在。
             这个状态栏计数留着 —— IDE 不在前台时它仍是唯一线索。 -->
        <statusBarWidgetFactory
            id="CCoderPendingPermissions"
            implementation="com.ccoder.ui.PendingPermissionStatusBarFactory"/>

        <!-- 转写区里点文件名却打不开时的提示（见 OpenFileTarget）。
             这里是**非粘性**的：那只是"这一次没打开"，不是需要用户处理的
             待办 —— 留一张撕不掉的气球就成了打扰。 -->
        <notificationGroup
            id="CCoder"
            displayType="BALLOON"
            isLogByDefault="false"/>
        <!-- 同一条快捷键上平台也有一个（$Paste）：焦点在我们的输入框时，把这个排前面。
             **必须在 extensions 里** —— 写在顶层会被静默忽略（这一条差点没赶上）。 -->
        <actionPromoter implementation="com.ccoder.ui.ImagePastePromoter"/>

        <!-- 界面语言必须**在界面出现之前**读起来：最早取文案的两处（编辑器/项目树右键菜单的
             update()、状态栏 getDisplayName()）都早于工具窗口，而平台服务是懒实例化的。

             启动时推的那一次交给下面的 `applicationListeners`（LanguageStartup）——
             这里从前写的是 `preload="true"`，而**市场从 2026-09 起拒收**带 preload 的包
             （"Service preloading is deprecated … migrate to listeners"）：
             2026-09-20 上传 0.2.22 时被当场挡下，见计划文档 §八。

             **必须待在 <extensions> 里**（2026-09-20 事故）：写到顶层会被平台当成
             `Unknown element: applicationService` **静默丢掉** —— 服务没注册上，
             `getService()` 回 null，于是工具窗口在 createToolWindowContent 里 NPE、
             整个 CCoder 窗口打不开。`PluginXmlShapeTest` 把这类错钉死了。 -->
        <applicationService serviceImplementation="com.ccoder.settings.UiLanguageSettings"/>

        <!-- 启动时把界面语言推给词表层（就是上面那个 preload 的意思，换了个合法的写法）。
             注册语法照平台自己的描述符：<extensions> 里的 applicationListeners +
             topic=AppLifecycleListener（PlatformExtensions.xml 里 33 处都这么写）。 -->
        <applicationListeners>
            <listener class="com.ccoder.settings.LanguageStartup"
                      topic="com.intellij.ide.AppLifecycleListener"/>
        </applicationListeners>
    </extensions>

    <actions>
        <!-- 编辑器右键：把选中代码加到输入框。
             2026-09-14 起输入框里放的是**一行记号**（⟦路径 行范围 · N 行⟧），
             完整片段在发送时才展开 —— 选 30 行不再顶掉 30 行输入框。
             排在最上面（anchor="first"），没有选区时由 update() 置灰。 -->
        <action
            id="com.ccoder.AddSelectionToChat"
            icon="/icons/ccoderAction.svg"
            class="com.ccoder.ui.AddSelectionToChatAction"
            text="%action.addSelection.text"
            description="%action.addSelection.desc">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
        </action>

        <!-- 编辑器右键：整个文件。**没有选区时也能用** —— 想引用一整个文件，
             以前只能全选再右键，而全选出的是"这个文件的全部行"。 -->
        <action
            id="com.ccoder.AddFileToChat"
            icon="/icons/ccoderAction.svg"
            class="com.ccoder.ui.AddFileToChatAction"
            text="%action.addFile.text"
            description="%action.addFile.desc">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
        </action>

        <!-- 项目树右键：多选一次全加。在树里挑文件比"开文件再右键"自然得多。 -->
        <action
            id="com.ccoder.AddFilesToChat"
            icon="/icons/ccoderAction.svg"
            class="com.ccoder.ui.AddFilesToChatAction"
            text="%action.addFiles.text"
            description="%action.addFiles.desc">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="first"/>
        </action>

        <!-- Ctrl+V 贴图。**只在"焦点在输入框、剪贴板里只有图"时可见**（见
             PasteImageAction 的说明）—— 其余时候它不是候选，Ctrl+V 归平台原来那套，
             文字粘贴一个字不变。 -->
        <action
            id="com.ccoder.PasteImage"
            class="com.ccoder.ui.PasteImageAction"
            text="%action.pasteImage.text"
            description="%action.pasteImage.desc">
            <keyboard-shortcut keymap="$default" first-keystroke="control V"/>
        </action>
    </actions>

</idea-plugin>
