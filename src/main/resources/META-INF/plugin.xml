<idea-plugin>
    <id>com.ccoder.claudecode</id>
    <name>CCoder</name>
    <vendor>ccoder</vendor>

    <description><![CDATA[
    Claude Code integration for PyCharm via the Claude Agent SDK.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <!-- 转写区用内嵌浏览器渲染。该别名自 2025.3.1 起稳定。 -->
    <depends>com.intellij.modules.jcef</depends>

    <extensions defaultExtensionNs="com.intellij">
        <projectConfigurable
            id="com.ccoder.settings"
            displayName="CCoder"
            instance="com.ccoder.settings.ClaudeSettingsPanel"/>

        <toolWindow
            id="CCoder"
            anchor="right"
            icon="AllIcons.Toolwindows.ToolWindowMessages"
            factoryClass="com.ccoder.ClaudeToolWindowFactory"/>

        <!-- spec §6.3 的不可忽略性补偿：待决权限在状态栏常驻显示 -->
        <statusBarWidgetFactory
            id="CCoderPendingPermissions"
            implementation="com.ccoder.ui.PendingPermissionStatusBarFactory"/>

        <!-- 卡片插入时若工具窗口不可见，用粘性通知提醒。
             displayType 的合法值只有 BALLOON / STICKY_BALLOON /
             TOOL_WINDOW / NONE —— 写 STICKY 不是合法常量，
             平台会解析为 null 并拒绝注册整个组。 -->
        <notificationGroup
            id="CCoder Permissions"
            displayType="STICKY_BALLOON"
            isLogByDefault="false"/>

        <!-- 转写区里点文件名却打不开时的提示（见 OpenFileTarget）。
             这里是**非粘性**的：那只是"这一次没打开"，不是需要用户处理的
             待办 —— 留一张撕不掉的气球就成了打扰（上面那组的粘性是为
             待决权限准备的，语义不同，不能混用）。 -->
        <notificationGroup
            id="CCoder"
            displayType="BALLOON"
            isLogByDefault="false"/>
    </extensions>

    <actions>
        <!-- 编辑器右键：把选中代码带路径与行号追加到聊天输入框。
             排在最上面（anchor="first"），没有选区时由 update() 置灰。 -->
        <action
            id="com.ccoder.AddSelectionToChat"
            class="com.ccoder.ui.AddSelectionToChatAction"
            text="添加到 CCoder 聊天框"
            description="把选中的代码带路径与行号追加到 CCoder 输入框">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
        </action>
    </actions>
</idea-plugin>
