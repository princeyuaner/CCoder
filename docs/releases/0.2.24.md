**Install:** [JetBrains Marketplace](https://plugins.jetbrains.com/plugin/34281) ·
or download `CCoder-0.2.24.zip` below → Settings → Plugins → ⚙ → Install Plugin from Disk

- **Thinking can start collapsed** — a new setting folds thinking text by default,
  **including the block that is still streaming**; the spinner and timer stay in the
  title, and one click expands it. Off by default.
- **A font and size for the transcript** — under Settings → General → Interface: four
  families (plus "follow IDE") and four sizes. They apply to the transcript only; code
  blocks always stay monospaced, and the composer keeps following the IDE.
- **Model, permission mode and thinking effort open as pickers** — the three composer
  switches now share one card look, with group headings (permission modes that never
  ask again; effort levels only some models accept).
- **Session tabs come back the way you left them** — the tabs open at exit are restored
  with their session, model, title and which one was current. They used to live in
  memory only: five tabs reopened as one, and the rest had to be found again in the
  session list. A tab whose session cannot be reopened now says so instead of starting
  an empty conversation silently.
- **The model is remembered per tab** — with one app-wide selection, switching it
  relabelled every window and tab while only a single session had actually changed (the
  model is set in the sidecar's process environment at launch). Each tab now carries its
  own; a new tab starts from the model this project used last, and upgrading keeps the
  one you had.

`CCoder-0.2.24.zip` · 8,779,394 bytes ·
sha256 `c5ea746eb79742eddc179784eac03b37b3c9642ba74ed7da26e75325af3ec738`

---

**安装**：[JetBrains 市场](https://plugins.jetbrains.com/plugin/34281)，或下载下面的
`CCoder-0.2.24.zip` → 设置 → 插件 → ⚙ → 从磁盘安装插件

- **思考可以默认收起** —— 新开关让思考内容默认折叠，**正在流式的那块也收起**；标题里的
  转圈与秒表照旧，点一下标题即可展开。默认**不**折叠。
- **转录区能选字体与字号** —— 设置 →「通用」→「界面」卡：四档字体（含「跟随 IDE」）与
  四档字号，只作用在转录区；代码块始终等宽，输入框仍跟随 IDE。
- **模型 / 权限 / 思考深度换成弹层选择** —— 输入区那三个开关共用一套卡面，带分组标题
  （如「不再问你」「仅部分模型认」）。
- **会话标签按上次的样子回来** —— 退出时开着的那几个标签连会话、模型、标题、哪条是当前
  一起恢复。从前标签只活在内存里：开五个页签，回来只剩一条，其余得自己去会话列表里找回来。
  哪条会话打不开了会如实说一句，不再静默开个空会话。
- **模型按标签各记各的** —— 从前全应用一份选中态：切一次会让每个窗口、每个标签都跟着改口，
  而实际变的只有一条会话（模型是起 sidecar 时烤进进程环境的，改设置到不了别人那个跑着的
  会话）。现在每条标签记自己的；新标签从「本项目上次用的模型」开局，升级上来的沿用原样。
